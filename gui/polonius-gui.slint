import { TextEdit } from "std-widgets.slint";
export component MainWindow inherits Window {

    preferred-width: 300px;
    preferred-height: 200px;

    TextEdit {
        width: parent.width;
        height: parent.height;
        text: @tr("Polonious Gui Test 1");
    }
}